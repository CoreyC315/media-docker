# Secrets / local environment — never commit
.env

# Runtime app state (bind-mounted config for each service)
config/

# Local data root if anyone points it inside the repo
data/

# OS cruft
.DS_Store
